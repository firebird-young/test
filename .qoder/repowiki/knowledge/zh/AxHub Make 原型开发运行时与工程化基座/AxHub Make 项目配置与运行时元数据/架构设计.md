- 事实源层：`client.json` 定义唯一的项目 ID 与名称，`axhub.config.json` 提供服务器连接与项目默认配置。
- 派生缓存层：`project.json` 聚合原型（prototypes）与主题（themes）的元数据、路由及页面结构；`entries.json` 维护构建入口扫描缓存；`sidebar-tree.json` 记录侧边栏导航树。
- 运行状态层：`.dev-server-info.json` 等文件记录本地服务状态，`sessions/` 目录存储历史会话数据。
- 依赖方向：运行时工具读取事实源生成派生缓存，确保配置同步与界面渲染的一致性。
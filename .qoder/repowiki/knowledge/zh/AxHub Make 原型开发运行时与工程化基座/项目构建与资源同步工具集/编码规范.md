- 所有脚本均采用 ESM 格式（.mjs/.js），并通过 `fileURLToPath(import.meta.url)` 动态解析工作区根路径以确保跨环境兼容性。
- 涉及文件写入的操作（如 `writeJsonAtomic`）普遍采用“先写临时文件再原子重命名”的策略，防止构建中断导致元数据损坏。
- 命令行参数解析遵循统一的 `parseArgs` 模式，支持 `--dry-run` 预演、`--help` 帮助信息及位置参数与标志位的混合输入。